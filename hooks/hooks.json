{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}\"/skills/split-module/scripts/check_file_length.py --hook",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
